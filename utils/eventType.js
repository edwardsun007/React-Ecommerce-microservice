const Types = Object.freeze({
    PostCreate: 'PostCreate',
    CommentCreate:'CommentCreate'
});

module.exports = {Types}; // nodeJs by default expecting common JS syntax module, do need to use module.exports instead of exports